// We are selecting the comments_(specifid id) div and appending the comment partial
// Then we select the comment_content_(specific id) div
// and change it's value to an empty string.
// -- Adding new comments to list of comments and clearning out the form we typed in.
$('#comments_<%= @post.id %>').append("<%=j render 'comments/comment', post: @post, comment: @comment %>");
$('#comment_content_<%= @post.id %>').val('')
